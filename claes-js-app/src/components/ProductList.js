import { Link } from "react-router-dom";

const ProductList = ({ products }) => {
  return (
    <>
      <div>hei jeg er Productlist </div>
    </>
  );
};

export default ProductList;

/* {products.map((product) => (
          <div key={product.id}>{product.title}</div>
        ))} */
